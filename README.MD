To run the code and execute the pre-defined example supermarket checkout flow:
`gradlew run`

To run unit tests
`gradlew test` - test results are then stored in `/build/reports/tests/test/index.html`


**Feedback would be much appreciated! Thanks for taking the time to look at my solution**